name: flutter_rs_ffi_barrage_example
description: Demonstrates how to use the flutter_rs_ffi_barrage plugin.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: ^3.41.0

dependencies:
  flutter:
    sdk: flutter
  flutter_rs_ffi_barrage:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
